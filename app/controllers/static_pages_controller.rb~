class StaticPagesController < ApplicationController
	def index
		@issues = Issue.all
		@excited_list = Item.where(category:"excited").map(&:content)
		@worried_list = Item.where(category:"worried").map(&:content)
	end
end
